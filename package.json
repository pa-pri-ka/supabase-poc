{
  "name": "supabase-poc",
  "source": "webapp/index.html",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "parcel build",
    "prerestart": "rimraf dist",
    "restart": "parcel serve",
    "prestart": "rimraf dist",
    "start": "parcel serve --open"
  },
  "devDependencies": {
    "@parcel/reporter-json": "^2.16.4",
    "eslint": "^10.6.0",
    "parcel": "^2.16.4",
    "rimraf": "^6.1.3"
  }
}
